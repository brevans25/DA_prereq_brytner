# Assignment 0
## Create & Push a Practice Project to GitHub
- Create a project folder named **DA_prereq_brytner**
- Inside it, create two folders: **Data** and **Notebooks**
- In **Notebooks**, create a Jupyter Notebook named **prereq_notebook.ipynb**
- Save a screenshot of commands given in terminal and save it in the **Data** folder
- Initialize Git in **DA_prereq_brytner**
- Add and commit files
- Create an empty GitHub repository and connect to local repo
- Save a screenshot of GitHub repo landing page and terminal in the **Data** folder
- Open **DA_prereq_brytner** in VS Code using the terminal
- Open **prereq_notebook.ipynb**
- Add one Python code cell and run it
- Add one Markdown cell introducing myself
- Save the notebook
- Add, commit, and push changes
- Save a screenshot of VS Code and terminal in the **Data** folder
- Double check and make sure all screenshots are stored in the **Data** folder
- Add, commit, and push the updated **Data** directory
- Submit GitHub repository link.